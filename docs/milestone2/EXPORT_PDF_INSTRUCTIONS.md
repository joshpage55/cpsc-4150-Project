# Export proxy PDF for Canvas

1. Open `BACKEND_PROXY_ARCHITECTURE.html` in Chrome (double-click or drag into browser).
2. File → Print → Save as PDF.
3. Submit the PDF on Canvas.

Alternative: open `BACKEND_PROXY_ARCHITECTURE.md` in VS Code and use a Markdown PDF extension.
