# Team secrets — copy to `.env` (never commit `.env`)
# Key source: private team Canvas announcement only

# OpenAI — mandated mini-tier model for Story Builder
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Firebase project
FIREBASE_PROJECT_ID=cpsc4150-readright

# M3 STT proxy — set as Firebase secret DEEPGRAM_API_KEY (not in Flutter)
# DEEPGRAM_API_KEY=
