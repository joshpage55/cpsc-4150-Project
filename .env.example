# Team secrets — copy to `.env` (never commit `.env`)
# Key source: private team Canvas announcement only

# Google Gemini — mandated mini-tier model for M2 spike
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash-lite

# Optional: Firebase project when team migrates off inherited readright-2ad31
# FIREBASE_PROJECT_ID=

# Future M3 proxies (do not add keys to Flutter)
# DEEPGRAM_API_KEY=
